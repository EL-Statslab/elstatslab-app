name: Keep Streamlit Awake
on:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://elstatslab.streamlit.app > /dev/null
